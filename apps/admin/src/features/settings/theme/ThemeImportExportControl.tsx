/**
 * Deferred import/export UI.
 *
 * This child control will call the pure serialization helpers, display
 * validation errors, and place valid imported settings into the parent draft.
 * Import must not auto-save. Export must serialize only the supported,
 * versioned ThemeSettings contract.
 * Do not wire or export this file until that contract exists.
 */
